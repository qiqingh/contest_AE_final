<!-- Page 144 of ts_138211v180200p -->

3GPP TS 38.211 version 18.2.0 Release 18
143
ETSI TS 138211 V18.2.0 (2024-05)
\[
S_{\text {start }}=\left\{\begin{array}{cl}
N_{\mathrm{s}}^{\mathrm{RIM}, 1} / 2 & \text { if EnoughIndication is enabled and 'enough mitigation' is to be indicated } \\
0 & \text { otherwise }
\end{array}\right.
\]
where \( N_{\mathrm{s}}^{\mathrm{RIM} 1} \) is the number of candidate sequences assigned for RIM-RS type 1
- \( R_{i} \) is the number of consecutive uplink-downlink periods for RIM-RS type \( i \) as given by clause 7.4.1.6.4.2;
- \( \bar{r} \in\left\{0,1, \ldots, R_{i}-1\right\} \).

The set ID is determined from the resource triplet according to
\[
n_{\text {setID }}=\left(i_{\mathrm{s}}^{\mathrm{RIM}}-S_{\text {start }}\right)+N_{\mathrm{s}}^{\mathrm{RIM}}\left\lfloor\frac{i_{\mathrm{t}}^{\mathrm{RIM}}-T_{\text {start }}}{R_{i}}\right\rfloor+N_{\mathrm{t}}^{\mathrm{RIM}} N_{\mathrm{s}}^{\mathrm{RIM}} i_{\mathrm{f}}^{\mathrm{RIM}}
\]
7.4.1.7 Positioning reference signals
7.4.1.7.1 General

A positioning frequency layer consists of one or more downlink PRS resource sets, each of which consists of one or more downlink PRS resources as described in [6, TS 38.214].
7.4.1.7.2 Sequence generation

The UE shall assume the reference-signal sequence \( r(m) \) is defined by
\[
r(m)=\frac{1}{\sqrt{2}}(1-2 c(2 m))+j \frac{1}{\sqrt{2}}(1-2 c(2 m+1))
\]
where the pseudo-random sequence \( c(i) \) is defined in clause 5.2.1. The pseudo-random sequence generator shall be initialised with
\[
c_{\mathrm{injt}}=\left(2^{22}\left|\frac{n_{\mathrm{JD}, \mathrm{seq}}^{\mathrm{PRS}}}{1024}\right|+2^{10}\left(N_{\mathrm{symb}}^{\text {slot }} n_{\mathrm{s}, \mathrm{f}}^{\mu}+l+1\right)\left(2\left(n_{\mathrm{ID}, \mathrm{seq}}^{\mathrm{PRS}} \bmod 1024\right)+1\right)+\left(n_{\mathrm{JD}, \mathrm{seq}}^{\mathrm{PRS}} \bmod 1024\right)\right) \bmod 2^{31}
\]
where \( n_{\mathrm{s}, \mathrm{f}}^{\mu} \) is the slot number, the downlink PRS sequence ID \( n_{\mathrm{ID}, \text { seq }}^{\mathrm{PRS}} \in\{0,1, \ldots, 4095\} \) is given by the higher-layer parameter \( d l \)-PRS-SequencelD, and \( l \) is the OFDM symbol within the slot to which the sequence is mapped.
7.4.1.7.3 Mapping to physical resources in a downlink PRS resource

For each downlink PRS resource configured, the UE shall assume the sequence \( r(m) \) is scaled with a factor \( \beta_{\text {PRS }} \) and mapped to resources elements \( (k, l)_{p, \mu} \) according to
\[
\begin{aligned}
a_{k, l}^{(p, \mu)} & =\beta_{\mathrm{PRS}} r(m) \\
m & =0,1, \ldots \\
k & =m K_{\text {comb }}^{\mathrm{PRS}}+\left(\left(k_{\text {offset }}^{\mathrm{PRS}}+k^{\prime}\right) \bmod K_{\text {comb }}^{\mathrm{PRS}}\right) \\
l & =l_{\text {start }}^{\mathrm{PRS}}, l_{\text {start }}^{\mathrm{PRS}}+1, \ldots, l_{\text {start }}^{\mathrm{PRS}}+L_{\mathrm{PRS}}-1
\end{aligned}
\]
when the following conditions are fulfilled:
- the resource element \( (k, l)_{p, \mu} \) is within the resource blocks occupied by the downlink PRS resource for which the UE is configured;
- the symbol \( l \) is not used by any SS/PBCH block used by a serving cell for downlink PRS transmitted from the same serving cell or any SS/PBCH block from a non-serving cell whose time frequency location is provided to the UE by higher layers for downlink PRS transmitted from the same non-serving cell;
- the slot number satisfies the conditions in clause 7.4.1.7.4.
and where

ETSI