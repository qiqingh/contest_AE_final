<!-- Page 115 of ts_138211v180200p -->

3GPP TS 38.211 version 18.2.0 Release 18
114
ETSI TS 138211 V18.2.0 (2024-05)

If \( b_{\text {hop }} \geq B_{\text {SRS }} \), frequency hopping is disabled and the frequency position index \( n_{b} \) remains constant (unless reconfigured) and is defined by
\[
n_{\mathrm{b}}=\left\lfloor 4 n_{\mathrm{RRC}} / m_{\mathrm{SRS}, b}\right\rfloor \bmod N_{\mathrm{b}}
\]
for all \( N_{\text {symb }}^{\text {SRS }} \) OFDM symbols of the SRS resource. The quantity \( n_{R R C} \) is given by the higher-layer parameter freq DomainPosition if configured, otherwise \( n_{\mathrm{RRC}}=0 \), and the values of \( m_{\mathrm{SRS}, b} \) and \( N_{\mathrm{b}} \) for \( b=B_{\mathrm{SRS}} \) are given by the selected row of Table 6.4.1.4.3-1 corresponding to the configured value of \( C_{\text {SRS }} \).

If \( b_{\text {hop }}<B_{\text {SRS }} \), frequency hopping is enabled and the frequency position indices \( n_{b} \) are defined by
\[
n_{b}=\left\{\begin{array}{ll}
\left\lfloor 4 n_{\mathrm{RRC}} / m_{\mathrm{SRS}, b}\right\rfloor \bmod N_{\mathrm{b}} & b \leq b_{\mathrm{hop}} \\
\left(F_{b}\left(n_{\mathrm{SRS}}\right)+\left\lfloor 4 n_{\mathrm{RRC}} / m_{\mathrm{SRS}, b}\right\rfloor\right) \bmod N_{\mathrm{b}} & \text { otherwise }
\end{array}\right.
\]
where \( N_{\mathrm{b}} \) is given by Table 6.4.1.4.3-1,
\[
F_{b}\left(n_{\mathrm{SRS}}\right)=\left\{\begin{array}{cc}
\left(N_{b} / 2\right)\left\lfloor\frac{n_{\mathrm{SRS}} \bmod \Pi_{b=b_{\mathrm{ras}}}^{b} N_{b^{\prime}}}{\Pi_{b=b_{\mathrm{ras}}}^{b-1} N_{b}}\right\rfloor+\left\lfloor\frac{n_{\mathrm{SRS}} \bmod \Pi_{b=b_{\mathrm{bon}}}^{b} N_{b}}{2 \Pi_{b=b_{\mathrm{ras}}}^{b-1} N_{b}}\right\rfloor & \text { if } N_{b} \text { even } \\
\left.\left\lfloor N_{b} / 2\right\rfloor n_{\mathrm{SRS}} / \Pi_{b=b_{\mathrm{ran}}}^{b-1} N_{b^{\prime}}\right\rfloor & \text { if } N_{b} \text { odd }
\end{array}\right.
\]
and where \( N_{b_{\text {hop }}}=1 \) regardless of the value of \( N_{\mathrm{b}} \). The quantity \( n_{\text {SRS }} \) counts the number of SRS transmissions. For the case of an SRS resource configured as aperiodic by the higher-layer parameter resourceType, it is given by \( n_{\text {SRS }}= \left[l^{\prime} /(s R)\right] \) within the slot in which the \( N_{\text {symb }}^{\text {SRS }} \) symbol SRS resource is transmitted. The quantity \( s \) is given by \( s=2 \) if the higher-layer parameter nrofSRS-Ports-n8 equals 'ports8tdm', otherwise \( s=1 \). The quantity \( R \leq N_{\text {symb }}^{\mathrm{SRS}} / s \) is the repetition factor given by the field repetition Factor if configured, otherwise \( R=N_{\text {symb }}^{\text {SRS }} \) -
For the case of an SRS resource configured as periodic or semi-persistent by the higher-layer parameter resourceType, the SRS counter is given by
\[
n_{\mathrm{SRS}}=\left(\frac{N_{\mathrm{slot}}^{\mathrm{frame}, \mu} n_{\mathrm{f}}+n_{\mathrm{s}, \mathrm{f}}^{\mu}-T_{\mathrm{offset}}}{T_{\mathrm{SRS}}}\right)\left(\frac{N_{\mathrm{symb}}^{\mathrm{SRS}}}{s R}\right)+\left\lfloor\frac{l^{\prime}}{s R}\right\rfloor
\]
for slots that satisfy ( \( N_{\text {slot }}^{\text {rame }, "} n_{\Gamma}+n_{s, \Gamma}^{\mu}-T_{o \Gamma l s e t} \) ) \( \bmod T_{\text {SRS }}=0 \). The periodicity \( T_{\text {SRS }} \) in slots and slot offset \( T_{\text {offset }} \) are given in clause 6.4.1.4.4.

ETSI