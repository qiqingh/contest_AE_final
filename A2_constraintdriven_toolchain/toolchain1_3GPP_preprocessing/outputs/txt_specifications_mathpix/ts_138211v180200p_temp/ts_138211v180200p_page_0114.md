<!-- Page 114 of ts_138211v180200p -->

3GPP TS 38.211 version 18.2.0 Release 18
113
ETSI TS 138211 V18.2.0 (2024-05)

if the higher-layer parameter EnableStartRBHopping is configured, otherwise \( k_{\mathrm{hop}}=0 \).
- \( \quad m_{\text {overlap }}^{\text {hop }} \in\{0,1,2,4\} \) is given by the higher-layer parameter YYY.
- \( n_{\text {SRS }}^{\text {TxHopping }}=0,1, \ldots, N_{\text {hops }}^{\text {SRS }}-1 \) is the hop transmission counter in the time domain, which corresponds to the order of the higher-layer parameter SlotOffsetForRemainingHops.
- \( n_{\text {init }}^{\text {hop }}=\left\lfloor n_{\text {shift }} /\left(m_{\text {SRS }, 0}-m_{\text {overlap }}^{\text {hop }}\right)\right\rfloor \) is the initial hop index.

The quantity \( f_{\text {coh }}\left(n_{\mathrm{f}}, n_{\mathrm{s}, \mathrm{f}}^{\mu}, l^{\prime \prime}\right) \) is given by
- if the higher-layer parameter combOffsetHopping is not configured:
\[
f_{\mathrm{coh}}\left(n_{\mathrm{f}}, n_{\mathrm{s}, \mathrm{f}}^{\mu} l^{\prime \prime}\right)=0
\]
- if the higher-layer parameter combOffsetHopping is configured:
\[
\begin{array}{l}
f_{\mathrm{coh}}\left(n_{\mathrm{f}}, n_{\mathrm{s}, \mathrm{f}}^{\mu}, l^{\prime \prime}\right)= \\
\left.s_{\mathrm{coh}}^{\mathrm{SRS}}\left(\sum_{m=0}^{7}\left(c\left(8\left(\left(n_{\mathrm{f}} \bmod 128\right) N_{\mathrm{slot}}^{\mathrm{frame}, \mu} N_{\mathrm{symb}}^{\mathrm{slot}}+n_{\mathrm{s}, \mathrm{f}}^{\mu} N_{\mathrm{symb}}^{\text {slot }}+l_{0}+l^{\prime \prime}\right)+m\right) 2^{m}\right)\right) \bmod n_{\mathrm{coh}}^{\mathrm{SRS}}\right)
\end{array}
\]
where \( s_{\text {coh }}^{\mathrm{SRS}}(n) \) and \( n_{\text {coh }}^{\mathrm{SRS}} \) is the \( (n+1) \) th entry and the cardinality of the set
\[
\mathcal{S}_{\mathrm{coh}}=\left\{s_{\mathrm{coh}}^{\mathrm{SRS}}(0), s_{\mathrm{coh}}^{\mathrm{SRS}}(1), \ldots, s_{\mathrm{coh}}^{\mathrm{SRS}}\left(n_{\mathrm{coh}}^{\mathrm{SRS}}-1\right)\right\}
\]
respectively, where \( \mathcal{S}_{\text {coh }} \) is given by the higher-layer parameter hoppingSubset in the combOffsetHopping [E if configured, otherwise \( \mathcal{S}_{\text {coh }}=\left\{0,1, \ldots, K_{T C}-1\right\} \). The higher-layer parameter hoppingSubset in the combOffsetHopping [E includes a bitmap of \( K_{\mathrm{TC}} \) bits with \( 1<n_{\mathrm{coh}}^{\mathrm{SRS}}<K_{\mathrm{TC}} \) non-zero bits, where if the \( (n+1) \) th non-zero bit is the \( t \) : th bit in the bitmap, then \( S_{\text {coh }}^{\text {SRS }}(n)=t-1 \).

The pseudo-random sequence \( c(i) \) is defined by clause 5.2 .1 and shall be initialized with \( c_{\text {init }}=n_{1 \mathrm{D}}^{\text {h op }} \) at the beginning of each radio frame for which \( n_{\mathrm{f}} \bmod 128=0 \), where the comb offset hopping identity \( n_{\mathrm{JD}}^{\mathrm{h}} \) op is contained in the higher-layer parameter combOffsetHopping.

If the higher-layer parameter hoppingWithRepetition is set to Repetition, \( l^{\prime \prime}=\left[l^{\prime} / R\right] R \), otherwise \( l^{\prime \prime}=l^{\prime} \).

If SRShoppingNrofHops is configured:
- The reference point for \( k_{0}^{\left(p_{i}\right)}=0 \) is the lowest subcarrier of the configured bandwidth for SRS with Tx hopping configured by the parameter XXX in TxhoppingBandwidth.
otherwise:
- If \( N_{\mathrm{BWP}}^{\mathrm{start}} \leq n_{\text {shift }} \) the reference point for \( k_{0}^{\left(p_{i}\right)}=0 \) is subcarrier 0 in common resource block 0 , otherwise the reference point is the lowest subcarrier of the BWP.

If the SRS is configured by the [E SRS-PosResource, the quantity \( k_{\text {offset }}^{l^{\prime}} \) is given by Table 6.4.1.4.3-2, otherwise \( k_{\text {offset }}^{l^{\prime}}=0 \).

The frequency domain shift value \( n_{\text {shift }} \) adjusts the SRS allocation with respect to the reference point grid and is contained in the higher-layer parameter freqDomainShift in the SRS-Resource [E or the SRS-PosResource [E. The transmission comb offset \( \bar{k}_{\mathrm{TC}} \in\left\{0,1, \ldots, K_{\mathrm{TC}}-1\right\} \) is contained in the higher-layer parameter transmission \( C o m b \) in the SRS-Resource [E or the SRS-PosResource [E and \( n_{b} \) is a frequency position index.

Frequency hopping of the sounding reference signal is configured by the parameter \( b_{\text {hop }} \in\{0,1,2,3\} \), given by the field \( b \)-hop contained in the higher-layer parameter freqHopping if configured, otherwise \( b_{\text {hop }}=0 \).

ETSI